# Rule to Follow when building for features

- Feature name will be carmal case; example: AppDrawer
- Each features has its own redux state.
- Each features should be reusable.
- If features need custom components create components folder in feature folder.
- use hooks don't use state directly.
