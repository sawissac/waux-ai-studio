import { ToolBuilder } from "@/features/ToolBuilder";

/** Studio route — mounts the WauxAiStudio workspace (the node-chain editor). */
export default function StudioPage() {
  return <ToolBuilder />;
}
